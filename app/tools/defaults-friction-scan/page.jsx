import DefaultsFrictionScan from "@/components/DefaultsFrictionScan";

export const metadata = {
  title: "2-Minute Defaults + Friction Scan | Human OS",
};

export default function DefaultsFrictionScanPage() {
  return <DefaultsFrictionScan />;
}
